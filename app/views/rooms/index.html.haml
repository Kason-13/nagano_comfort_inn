=provide(:title, 'All Rooms')
.center-form
  %h5 Our rooms!
  -if is_admin?
    =link_to 'Create a new room', new_admin_room_path, class: "btn btn-small btn-success"

=render 'rooms/room_list', display_extra: false
