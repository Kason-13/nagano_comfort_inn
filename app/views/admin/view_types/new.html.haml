=provide(:title, 'Add view type')
.row
  .center-form.sm-width
    =form_for([:admin,@view_type]) do |f|
      =render 'shared/error_messages', object: f.object
      =render 'fields' , f:f
      =f.submit "Create new view type", class:"btn btn-large btn-primary"
